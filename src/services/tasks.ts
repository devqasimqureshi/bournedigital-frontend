import { TaskType } from "interfaces";

const tasks = {
    get: async () => {
    },
    add: async (task: TaskType) => {
    },
    update: async (task: TaskType) => {
    },
    delete: async (task: TaskType) => {
    }
}

export default tasks
